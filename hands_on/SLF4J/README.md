# SLF4J logging example

This Maven project demonstrates `ERROR` and `WARN` messages using SLF4J with Logback.

Run it from this folder:

```powershell
mvn compile exec:java -Dexec.mainClass=LoggingExample
```
